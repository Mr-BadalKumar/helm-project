apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devtron-project.fullname" . }}-test-connection"
  labels:
    {{- include "devtron-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devtron-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
